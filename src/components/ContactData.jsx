// import use state
import { useEffect, useState } from "react";

// import id
// create component to display specific contact data
const ContactInfo = () => {
};
  // test if api link can get info with id. yes it can

// export component

export default ContactInfo;
